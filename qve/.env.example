# API key for authenticating requests to the PHP backend (WIN-203)
# Must match API_AUTH_KEY in wineapp-config/config.local.php
# Generate with: php -r "echo bin2hex(random_bytes(32));"
PUBLIC_API_KEY=your-api-key-here
